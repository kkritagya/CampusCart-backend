PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/campuscart
JWT_SECRET=replace-with-a-long-random-secret
CLIENT_ORIGIN=http://localhost:3000
ADMIN_EMAIL=admin@example.com
PASSWORD_RESET_URL=http://localhost:3000/reset_password

# SMTP credentials used for password-reset emails.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM=CampusCart <no-reply@example.com>

# eSewa ePay v2. "test" uses eSewa's official UAT credentials and endpoints.
# For production, set your merchant product code and secret supplied by eSewa.
ESEWA_ENVIRONMENT=test
ESEWA_PRODUCT_CODE=EPAYTEST
ESEWA_SECRET_KEY=8gBm/:&EnhH.1/q
